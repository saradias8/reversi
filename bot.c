#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "estado.h"
/*
ESTADO bot(ESTADO e, char *cmd2) {
  int i, j, x = 0, y, l, c, r;
  ESTADO tmp;
  if (*cmd2 == 'O') e.peca = VALOR_O;
  else if (*cmd2 == 'X') e.peca = VALOR_X;
  for (i = 0; i < 8; i++)
  
  return r;
}*/
